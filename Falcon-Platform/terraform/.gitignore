# Local .terraform directories
**/.terraform/*

# .tfstate files (Contains sensitive data in plain text!)
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# We will use *.auto.tfvars for non-sensitive defaults if needed
**/*.tfvars
**/*.tfvars.json

# Ignore override files as they are usually local customizations
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc